import React from 'react'

export const Paginate = (currentPage, postsPerPage) => {
    return (        //1. calculate, the number of rows per page based on what we set staet to

        <div>
            
        </div>
    )
}

